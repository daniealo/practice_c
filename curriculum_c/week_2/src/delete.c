//#include "mysql_function.h"
//#include "crud.h"



void delete(){
    /*
    int search;
    printf("삭제하실 학생번호를 입력하세요: ");
    scanf("%d",&search);
    

    struct student *check = head;
    struct student *prev = NULL;

    while(check != NULL ){
        if(check -> id == search){
            if(head == check){
                if(head -> next == NULL){
                    head = tail = NULL;                         
                }else {
                    head = head -> next;
                }
            }else if (tail == check){
                prev -> next = NULL;
                tail = prev;
            }else {
                prev -> next = check -> next;
            }
            free (check);
            printf("해당학생의 정보가 삭제되었습니다.\n");
            break;
        }
        prev = check;
        check = check -> next;
    }
    printf("해당학번의 학생을 찾을 수 없습니다.\n");
*/
}